#!/bin/bash

# Launch fastfetch and wait for a keypress
exec "$(dirname "$0")/niri-launch-or-focus-tui" --floating --center --name About "bash -c 'rxfetch-rs 2>/dev/null || fastfetch || neofetch || echo \"Niruv Shell\"; read -n 1 -s'"
